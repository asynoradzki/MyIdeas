import styled from "styled-components";

export const RegisterSelect = styled.select`
    width: 380px;
    font-size: 18px;
    height: 34px;
    padding-left: 10px;
    border-radius: 16px;
    border: none;

    &:focus {
        outline: nove;
        border: 1px;
    }
`